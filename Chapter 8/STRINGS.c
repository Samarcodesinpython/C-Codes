#include <stdio.h>

int main(){
    // char st[] = { 'a', 'b', 'c', 'd', '\0'};
    char st1[]= "abcd";
    // for(int i = 0; i <= 4 ; i++){
    //     printf("%c ", st);
    // }
    printf("The String is : %s", st1);
    return 0;
}